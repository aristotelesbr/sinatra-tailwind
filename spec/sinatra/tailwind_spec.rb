# frozen_string_literal: true

RSpec.describe Sinatra::Tailwind do
  it "has a version number" do
    expect(Sinatra::Tailwind::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(true).to eq(true)
  end
end
